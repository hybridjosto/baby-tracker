{
  # This file is a fallback for a Caddy-based tailnet proxy.
  # The active setup uses Tailscale Serve:
  #   /baby -> http://127.0.0.1:8000/baby
  auto_https off
}

:8443 {
  handle /baby* {
    reverse_proxy 127.0.0.1:8000
  }

  respond "Not Found" 404
}
