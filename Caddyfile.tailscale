{
  # Tailscale IPs are private; disable auto HTTPS to keep HTTP-only.
  auto_https off
}

:8443 {
  bind 100.113.227.1

  @probe_or_api path /healthz /api*
  handle @probe_or_api {
    reverse_proxy 100.113.227.1:8000
  }

  respond "Not Found" 404
}
